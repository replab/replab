function f = factorial(n)
% Returns the factorial of an integer n
%
% Args:
%   n (integer): Integer to compute the factorial of
%
% Returns:
%   vpi: Factorial
    f = replab.util.multiplyIntegers(1:n);
end
